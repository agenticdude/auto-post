# === Google OAuth Installed App ===
# Path to your OAuth client secrets JSON (download from Google Cloud Console > Credentials)
GOOGLE_OAUTH_CLIENT_SECRETS=client_secret.json

# Where to store the OAuth token.json (is created automatically after you sign in)
GOOGLE_OAUTH_TOKEN_DIR=.tokens

# Optional: override default FFmpeg command template
# Must contain {input} and {output}
#FFMPEG_COMMAND_TEMPLATE=ffmpeg -y -i "{input}" -vf "scale=1280:-2" -c:v libx264 -preset veryfast -crf 23 -c:a aac -b:a 128k "{output}"
